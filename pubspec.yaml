name: whatsapp_reactions
description: WhatsApp Reactions is a feature introduced in the popular messaging application WhatsApp that allows users to express their emotions or reactions to a message without typing a reply. It enhances the messaging experience by providing a quick and convenient way to acknowledge or respond to a message using visual cues.

version: 0.0.3
homepage: https://github.com/kapilsharma-gts/whatsapp_reactions

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - images/
